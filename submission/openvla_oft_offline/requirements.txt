# PyPI packages only. Do not add git/http/local-path dependencies.
#
# The scoring kit installs this file into its own interpreter
# (validate_submission.py runs `sys.executable -m pip install -r requirements.txt`),
# and that interpreter already has `torch==2.11.0+cpu` from the kit's setup.sh.
# Without an explicit pin the 7B policy would therefore run on CPU and blow the
# 10s per-request limit. The scoring environment is Python 3.10 on Ubuntu 22.04
# with a single NVIDIA L4 24GB.
#
# --index-url and --extra-index-url are rejected by the validator, so the CUDA
# build has to come from plain PyPI, where the linux x86_64 `torch` wheel is
# already CUDA-enabled. 2.2.0 matches the version OpenVLA-OFT trains against.
torch==2.2.0
fastapi>=0.68
uvicorn>=0.15
msgpack>=1.0
numpy==1.26.4
transformers==4.40.1
tokenizers==0.19.1
timm==0.9.10
peft==0.11.1
accelerate>=0.25.0,<1.0
# 0.1.99 ships wheels only up to cp311. The scoring image is Python 3.10 so it
# would install there, but it cannot build on the 3.12 Colab runtime used to
# measure this package. 0.2.0 has wheels for both, and matches what
# patch_openvla_oft_dependencies.py selects for training.
sentencepiece==0.2.0
safetensors>=0.4.3
einops>=0.7,<1.0
Pillow>=10,<12
