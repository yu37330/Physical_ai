"""Physical AI project source package.

Keeping this as a regular package, rather than an implicit namespace package,
allows TensorFlow Datasets to resolve custom builder source locations reliably.
"""
